# Python d

#extension - remote repositories

> settings user JSON
vložit 
    "git.path": "G:/win32app/git_portable/cmd/git.exe",
    "git.enabled": true,

poté otevřu git bash a dám 

git config --global user.email "koudelkamatje@seznam.cz"
git config --global user.name "koudelkaMatej"